server:
  port: 8075


eureka:
  instance:
    hostname: localhost
  dashboard:
    enabled: true
  client:
    fetchRegistry: false
    registerWithEureka: false
    serviceUrl:
      defaultZone: "http://${eureka.instance.hostname}:${server.port}/eureka/"




